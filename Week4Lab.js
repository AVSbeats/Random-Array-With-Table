/*
    Name: Abraham Vallejos-Soto
    Date: 3/30/2023
    Assignment: Week 4 Lab
*/

// Part 1:

var a = []; // This is setting an array using 'a' as a var
var b = []; // This is setting an array using 'b' as a var

for (i=0; i<=10; i++) { // This is a for loop that will have a total of 11 repeats
    
    a[i] = Math.random(); // This means that a random math number will be displayed inside the 'a' array length
    b[i] = Math.random(); // This means that a random math number will be displayed inside the 'b' array length
    
}

console.log(a); //This will display the results of 'a' using node.js powershell inside Visual Studio Code
console.log(b); //This will display the results of 'b' using node.js powershell inside Visual Studio Code